Verify each finding against the current code and only fix it if needed.

In `@backend/Src/PIED_LMS.Presentation/Extensions/OpenApiConventionExtensions.cs`
around lines 100 - 103, The check that inspects result.Count == 0 and adds
StatusCodes.Status200OK is dead/unreachable; inside the method in
OpenApiConventionExtensions where you iterate over the incoming statusCodes (the
foreach that populates the HashSet result), remove the unreachable if-block that
checks result.Count == 0 and the call to result.Add(StatusCodes.Status200OK) so
the method relies solely on the existing early return when statusCodes is
null/empty and the HashSet population logic.


Verify each finding against the current code and only fix it if needed.

In `@backend/Src/PIED_LMS.Presentation/Extensions/OpenApiConventionExtensions.cs`
around lines 89 - 98, Replace the manual deduplication loop in
OpenApiConventionExtensions (the block using variables result and seen over
statusCodes) with a LINQ distinct approach: remove the HashSet seen and foreach
loop and assign result from statusCodes.Distinct() (or
Enumerable.Distinct(statusCodes)) converted to a list, preserving the same int
list semantics.

Verify each finding against the current code and only fix it if needed.

In `@backend/Src/PIED_LMS.Presentation/APIs/TestRoomEndpoints.cs` around lines 20
- 24, The POST endpoint CreateTestRoom currently returns 200 via ToActionResult;
to follow REST conventions change the creation handler to return 201 Created:
modify the CreateTestRoom implementation to use Results.Created(location,
result) (include a location URI that points to the new resource, e.g.,
"/testrooms/{id}" or use route name), or alternatively extend ToActionResult to
accept a creation flag and map successful create results to Results.Created;
update the CreateTestRoom call site to supply that flag or call Results.Created
directly so the endpoint returns 201 instead of 200.


Verify each finding against the current code and only fix it if needed.

In `@backend/Src/PIED_LMS.Presentation/APIs/TestCaseEndpoints.cs` around lines 17
- 20, The route declaration for CreateTestCase uses
ServiceResponseStatusProfile.OkOrBadRequestOrForbidden which is incorrect;
change the response profile to a POST-appropriate profile that includes 404
(e.g. ServiceResponseStatusProfile.CreatedOrBadRequestOrNotFound) or to
ServiceResponseStatusProfile.CreatedOrBadRequest if you will not return 404, and
update the CreateTestCase handler (the success path that returns
TestCaseResponse) to return a 201 Created semantics (e.g., Created result) so
the OpenAPI/profile and actual handler responses (CreateTestCase,
TestCaseResponse, ServiceResponseStatusProfile) are consistent.

Verify each finding against the current code and only fix it if needed.

In `@backend/Src/PIED_LMS.Presentation/APIs/ExamRoomEndpoints.cs` around lines 64
- 67, The OpenAPI docs for the endpoint defined by
group.MapGet("/{roomId}/exams/student", GetExamsInRoomForStudent) are incorrect
because route parsing can return 400 for non-GUID roomId; update the endpoint's
documented responses by replacing ServiceResponseStatusProfile.OkOrForbidden
with ServiceResponseStatusProfile.OkOrBadRequestOrForbidden in the
WithServiceResponseOpenApi<List<Contract.Services.Exam.ExamInRoomResponse>>(...)
call; optionally also tighten the route to "{roomId:guid}/exams/student" to
enforce GUIDs at routing time if you prefer preventing the 400 before the
handler.

Verify each finding against the current code and only fix it if needed.

In `@backend/Src/PIED_LMS.Presentation/APIs/AuthenticationEndpoints.cs` around
lines 51 - 54, The current endpoint mapping for GetUserById uses
ServiceResponseStatusProfile.OkOrNotFound but omits the model-binding failure
(400) that occurs when the {id} segment is not a GUID; update the mapping for
GetUserById to include the bad-request response profile (or replace OkOrNotFound
with a profile that includes BadRequest) so that WithServiceResponseOpenApi on
the GetUserById route documents 400 as well as 200/404; locate the MapGet call
that chains .WithName("GetUserById").RequireAuthorization(...) and change the
ServiceResponseStatusProfile argument (or add the bad-request profile)
accordingly.

Verify each finding against the current code and only fix it if needed.

In `@backend/Src/PIED_LMS.API/Filters/SecurityRequirementsOperationFilter.cs`
around lines 24 - 31, Both AllowsAnonymous and RequiresAuthorization repeatedly
access context.ApiDescription.ActionDescriptor.EndpointMetadata; extract this
into a single local variable or a small helper (e.g., GetEndpointMetadata or
pass endpointMetadata as a parameter) and use that variable in both private
static methods to avoid duplicate property access and null/count checks; update
AllowsAnonymous and RequiresAuthorization to use the shared endpointMetadata
variable and preserve existing null/empty handling and OfType checks
(IAllowAnonymous, IAuthorizeData).